# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# env – tajemství nikdy do repa
.env
.env.local
.env.*.local

# build / cache
*.tsbuildinfo
.turbo/
.vercel/

# Tento repozitář používá npm (package-lock.json). pnpm-lock.yaml by
# přinutil Vercel přepnout na pnpm, které nečte pole "workspaces" → build spadne.
pnpm-lock.yaml

# OS / editor
.DS_Store
